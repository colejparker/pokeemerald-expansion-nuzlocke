- [x] Players start with 750,000$ (lowered from 999,999 to leave headroom before hitting the 999,999 money cap)
- [x] Pokemon are randomized (built with `Zetraphes/pokeemerald-expansion@tertu-randomizer` as a reference; deviated from it where this codebase already had better-suited infrastructure — see `include/randomizer.h`/`src/randomizer.c` for the core engine)
    - [x] This includes wild encounters (`src/wild_encounter.c` — all 5 species-selection points hooked: `TryGenerateWildMon`, `GenerateFishingWildMon`, the Feebas special tile, and the read-only `GetLocalWildMon`/`GetLocalWaterMon` prediction paths used for DexNav/Pokédex hints, so those stay consistent with what actually appears), starter pokemon [x] (`src/starter_choose.c` `GetStarterPokemon` — the single function both the selection-screen sprite preview and `CB2_GiveStarter`'s actual give-mon logic already route through, so the existing 5-perfect-IV logic needed zero changes and automatically applies to whatever species comes out. New `RandomizeUniqueMonList` in `src/randomizer.c` retries on collision so the 3 starter choices can't accidentally roll the same species twice), and random trainers [x] (`src/battle_main.c` `CreateNPCTrainerPartyFromTrainer` — species substituted into one local var reused for gender/nature/ability lookups and `CreateMon`, not just the `CreateMon` call, and the ability-matching block falls back to "any valid ability slot for the new species" when randomized, instead of asserting against the original species' designed ability data. Seeded from the trainer's `struct Trainer` pointer (a stable, unique, compile-time-fixed ROM address — no numeric trainer ID is threaded through this call chain, so this stands in for one) + party slot, so a given trainer's randomized team is stable across fights on a save)
    - [x] This does not include gym battles, the Elite 4 and the Champion — refined per your call: Gym Leaders keep exactly one fixed Pokémon (their **last** party slot, which by this data's weakest-to-strongest convention is already their signature/ace mon — e.g. Wattson's last slot, Wallace's last slot when he was still a Gym Leader) and randomize the rest; Elite Four and Champion are fully randomized with no fixed slot at all, no exclusion
    - [x] Pokemon are randomized to be within 10% up or down of the base stats of the Pokemon that is being randomized. (`GetRandomizedSpecies` in `src/randomizer.c` — every randomizable species is loaded into a table sorted by base stat total once per session, then a binary search finds the `+/-10%` BST window and picks uniformly within it via a seeded RNG. Confirmed with you this means species-swap-by-power-level, not a per-individual stat multiplier. Randomization is deterministic — seeded from the save's trainer ID plus map/slot context, so the same wild-grass tile always gives the same species on a given save rather than re-rolling every encounter. Species eligibility (`IsSpeciesRandomizable`) excludes disabled/placeholder species and gimmick-only forms (Mega, Gigantamax, Totem, Primal Reversion, Ultra Burst, Tera-only, non-tradeable) via existing `SpeciesInfo` flags, plus a curated restricted-legendary ban list matching the list below (whole-family bans for Arceus/Kyurem/Zacian/Zamazenta/Giratina/Necrozma via base-species matching, exact-form bans for the rest, e.g. only the Calyrex Rider forms and only Hoopa Unbound)
    - [x] Abilities are randomized from a whitelisted list of abilities (`RandomizeAbility` in `src/randomizer.c`, hooked into the single canonical lookup `GetSpeciesAbility` in `src/pokemon.c` — every one of its ~30 call sites across battle AI, DexNav, the party menu Ability Capsule/Patch logic, Trace/Skill Swap checks, etc. gets the randomized ability for free with no call-site changes, since they all route through this one function. Broad whitelist, matching your call — every ability is eligible except a short list of form-change-locked ones (Zen Mode, Stance Change, Shields Down, Schooling, Disguise, Battle Bond, Power Construct, Gulp Missile, Hunger Switch) that are tied to specific species' form tables and would risk misbehaving on an unrelated species rather than just being "weird." `originalAbility == ABILITY_NONE` is always passed through unchanged, preserving "does this species even have a 2nd/3rd ability slot" checks used throughout that code)
        - [x] Abilities are maintained across evolutions (seeded off `GetBaseEvolutionSpecies` — walks pre-evolutions back to the family's root via the existing `GetSpeciesPreEvolution`, so every stage in a chain rolls the same result for a given ability slot)
        - [x] Abilities of Mega Evolutions are not maintained across evolutions and are pulled from a separate, stronger list of abilities (Mega forms are seeded independently by their own species id instead of the family root, from a curated ~55-ability pool of generally strong/impactful abilities in `src/randomizer.c`'s `sRandomizerStrongAbilityWhitelist` — flagging this list specifically for your review, "strong" is a balance judgment call and this is a first pass, not a final one)
        - [x] Shedinja always has Wonder Guard, but its type is randomized (ability lock is a species-id exception in `RandomizeAbility`. Type randomization is `RandomizeSpeciesType` in `src/randomizer.c`, hooked into `GetSpeciesType` in `src/pokemon.c` — investigated first since `gSpeciesInfo` is `const` (ROM-resident, can't be mutated at runtime) and species type looked like it might be read directly in ~80 places; turned out all but 2 real call sites already routed through `GetSpeciesType`, so wrapping that one function was enough. Fixed the 2 exceptions found along the way: `IsSpeciesOfType` in `pokemon.c` now calls `GetSpeciesType` instead of reading `gSpeciesInfo[].types[]` directly, and `RandomizeLevelUpLearnset`'s own STAB-type check does too, so Shedinja's synthesized moveset stays consistent with whatever type it rolls. Both type slots are rolled independently from the 18 real types (excludes the `TYPE_MYSTERY` placeholder and `TYPE_STELLAR`), so Shedinja can come out monotype or dual-type. Piggybacks on the ability-randomizer toggle rather than a new flag, matching how gameplan.md nests this under that bullet)
        - [x] Ditto always has Imposter (same ability-lock mechanism as Shedinja)
        - [x] A Gym Leader's ace (their fixed last party slot, already exempt from species randomization) is also exempt from ability randomization, so an explicit `Ability:` line in `src/data/trainers.party` actually sticks. Unlike Shedinja/Ditto this can't be a species-based exception in `RandomizeAbility` (the ace's species differs per Leader, and that same species could still appear elsewhere as a randomized substitute with its ability correctly randomized there) — needed a genuine per-individual lock instead. Added `cantRandomizeAbility:1` to `PokemonSubstruct3` (repurposed the spare `unused_0B` bit) and a new `MON_DATA_CANT_RANDOMIZE_ABILITY` accessor in `src/pokemon.c`, set on the ace slot in `CreateNPCTrainerPartyFromTrainer`/`src/battle_main.c` (same `trainerClass == TRAINER_CLASS_LEADER && i == monsCount - 1` condition already used for the species exemption). New `GetUnrandomizedAbilityBySpecies` in `pokemon.c` mirrors `GetAbilityBySpecies`'s empty-slot fallback search but reads the species' true ability table (via new `GetRawSpeciesAbility`) instead of going through `RandomizeAbility`; `GetMonAbility` checks the lock bit and picks between the two. Had to fix 2 other call sites that independently re-derived a battler's ability straight from `species`+`abilityNum` (bypassing `GetMonAbility` and its lock check) rather than reading it off the actual party mon: `battle_main.c`'s battle-intro sprite draw and `battle_script_commands.c`'s switch-in handler, both now call `GetMonAbility(GetBattlerMon(battler))` instead)
- [x] Movesets of Pokemon are randomized (`RandomizeLevelUpLearnset` in `src/randomizer.c`, hooked into the single canonical accessor `GetSpeciesLevelUpLearnset` in `src/pokemon.c` — the same wrap-the-one-choke-point pattern as abilities/TM moves, so every one of its ~15 call sites, from the party menu to Move Relearner to Battle Dome's move-rarity check, gets the synthesized learnset automatically. Uses reservoir sampling (Algorithm R) to pick each of the 3 move categories in a single pass over every move in the game, rather than building and sorting a ~900-move candidate list — keeps memory use to a handful of bytes per species instead of risking a stack overflow. One performance note worth knowing: `IsDomeRareMove` in `battle_dome.c` calls this once per species across the whole dex (~1500 calls) to build its move-rarity table — each call is a ~900-move scan, so that one-off Battle Dome setup will be doing meaningfully more work than before; hasn't caused issues in testing but flagging it since it's the one place this gets called at that scale)
    - [x] Every Pokemon learns 21 moves of at the same levels, with the last move being learned at Level 63 (`sMovesetLevelCurve` — 21 fixed levels from 1 to 63, spaced as evenly as 21 slots allow: 1,4,7,10,13,16,19,22,25,28,32,35,38,41,44,47,50,53,56,59,63)
    - [x] The move sets always contain 7 STAB moves, 7 non-stab moves and 7 non-damaging moves (interleaved round-robin across the 21 level slots — slot 0,3,6...=STAB, 1,4,7...=non-STAB, 2,5,8...=status — so the mix is spread across the whole learnset rather than clumped at one end. STAB/non-STAB is determined by comparing each move's type against the species' own type(s); "non-damaging" = `DAMAGE_CATEGORY_STATUS`. `MOVE_STRUGGLE` is excluded as a candidate for obvious reasons; HM moves are allowed to appear here (unlike the TM pool) since there's no dual-mapping ambiguity for a level-up slot)
    - [x] Moves become more powerful later into the learnset (the STAB and non-STAB picks are sorted ascending by `GetMovePower` before being assigned to their increasing-level slots; status moves have no comparable "power" stat so they're left in whatever order the random sampling produced)
    - [x] Every Pokemon can learn every TM and every HM (`src/pokemon.c` `CanLearnTeachableMove` — any move that currently maps to a TM or HM item via `GetTMHMItemIdFromMoveId` is universally learnable regardless of species; Move Tutor moves stay species-gated by the existing per-species `teachableLearnset` list. Deliberately keyed off "is this move taught by some TM/HM slot" rather than a hardcoded move list, so it stays correct once TM-to-move mappings get shuffled per-run)
- [x] Remove move tutors (the 10 vanilla Hoenn move-tutor NPCs — Swagger/Slateport Fan Club, Rollout/Mauville, Fury Cutter/Verdanturf, Mimic/Lavaridge, Metronome/Fallarbor, Sleep Talk/Fortree, Substitute/Lilycove Rooftop, Dynamic Punch/Mossdeep, Double-Edge/Sootopolis, Explosion/Pacifidlog — deleted their `object_events` entries from the 8 affected maps' `map.json` files (some NPCs share a map). `data/scripts/move_tutors.inc` and `data/text/move_tutors.inc` pruned down to just the shared "choose a box mon and teach it a move" framework (`MoveTutor_EventScript_OpenPartyMenu/OpenBox/CanOnlyBeLearnedOnce`, `MoveTutor_AfterChooseBoxMon`), since `src/chooseboxmon.c`'s `SELECT_PC_MON_MOVE_TUTOR` table entry still references `MoveTutor_AfterChooseBoxMon` at the C level regardless of whether any script calls it — the per-NPC dialogue/teach scripts and their text were deleted since nothing else referenced them. Left the FRLG-side move tutors (`move_tutors_frlg.inc`) and the `FLAG_MOVE_TUTOR_TAUGHT_*` flag IDs alone — the former are unreachable Kanto-only content in this Hoenn-only hack, the latter are harmless unused flag reservations not worth renumbering)
- [x] Items are randomized (`RandomizeFieldItem` in `src/randomizer.c`, hooked into `src/item_ball.c` `GetItemBallIdFromTemplate` for visible item balls and `src/field_control_avatar.c`'s `BG_EVENT_HIDDEN_ITEM` case for hidden items — the two choke points before an item id reaches `additem`/`finditem`. Seeded from map + the object event's/hidden item's local id, so a given pickup always resolves to the same item on a save)
    - [x] Any item given to the player is randomized from a list, with the exception of HM's and the key items needed for story progression (excludes the whole Key Items pocket, not just specific story items — randomizing any key item risks breaking progression or removing a QOL item, so the exclusion is blanket rather than curated). Items pull from your curated ~185-item pool in `src/data/randomizer/item_whitelist.h` (held items, berries, evolution stones, vitamins/wings) instead of any item in the game, replacing the initial "everything" implementation
    - [x] Mega Stones are in the item pool too, but only `RANDOMIZER_MEGA_STONE_POOL_SIZE` (10) of them are ever obtainable on a given save, not the whole ~92-item Mega Stone list (per your call). `BuildMegaStoneSubset`/`src/randomizer.c` scans every item for `GetItemHoldEffect() == HOLD_EFFECT_MEGA_STONE` and reservoir-samples 10 of them once per save (same Algorithm R technique as the moveset randomizer, so the full list never needs to be held in memory at once), seeded off the save's trainer ID like everything else - deterministic, not re-rolled. That 10-item subset is folded into `RandomizeItemFromSeed`'s combined pool alongside the curated whitelist and the TM range, so a Mega Stone can turn up from a field pickup, an NPC gift, or a randomized TM slot, same as any other item
    - [x] All berry trees are replaced by items a player can pick-up (per your clarification, this means a literal item ball object at each spot, not a simplified version of the growing/watering minigame. Scripted the conversion rather than hand-editing — found all 88 real berry tree placements across 16 `data/maps/Route{102-104,110-121,123,130}/map.json` files (a 89th tree-id constant, `BERRY_TREE_UNUSED`, was never placed on any map). Each placement's `graphics_id`/`movement_type`/`script`/`trainer_sight_or_berry_tree_id`/`flag` fields were rewritten from the berry-tree template (`OBJ_EVENT_GFX_BERRY_TREE`/`MOVEMENT_TYPE_BERRY_TREE_GROWTH`/`BerryTreeScript`) to the same template every other item ball on the map already uses (`OBJ_EVENT_GFX_ITEM_BALL`/`MOVEMENT_TYPE_LOOK_AROUND`/`Common_EventScript_FindItem`), holding the same berry that tile would've grown on a fresh save (derived from each tree-id constant's own name, e.g. `BERRY_TREE_ROUTE_104_CHERI_1` -> `ITEM_CHERI_BERRY` — cross-checked against `data/scripts/new_game.inc`'s `EventScript_ResetAllBerries`). 8 of the 88 tiles start as empty, never-planted soil with no natural berry to give (named `..._SOIL_*`); per your call, these also became item balls rather than being removed, holding a filler Oran Berry. Each of the 88 needed its own permanent "already picked up" flag (item balls need one to not respawn on reload, unlike berry trees which track state in the separate `berryTrees[]` save array) — repurposed 88 previously-unused `FLAG_UNUSED_0x0XX` slots in `include/constants/flags.h`, one per tile, named `FLAG_ITEM_BERRY_TREE_<original tree id>` for traceability. Left the underlying berry-growing engine itself untouched (`src/berry.c`, `BerryTreeScript`/`data/scripts/berry_tree.inc`, the `MOVEMENT_TYPE_BERRY_TREE_GROWTH` object movement type, `EventScript_ResetAllBerries`) — same precedent as the move-tutor removal (`src/chooseboxmon.c`'s dangling `SELECT_PC_MON_MOVE_TUTOR` reference): since no map object references any of it anymore, it's inert dead code rather than something worth a deeper purge. Verified with a full build after the conversion, not just the map data change)
    - [x] The TM's a player can get are randomized into any item (and any item can become a TM), HM's are kept the same (updated per your call — TMs no longer stay in their own separate TM-only pool; `RandomizeItemFromSeed`/`src/randomizer.c` now draws every substitution, TM or not, from one combined pool: the curated whitelist plus the ITEM_TM01-TM50 range folded in programmatically. HMs are still excluded from randomization entirely and always give the original HM. Which move a given TM slot teaches is unaffected — still a separate roll, see below)
    - [x] Which move each TM actually teaches is also randomized (this codebase only has 50 real TMs total — confirmed `FOREACH_TM` in `include/constants/tms_hms.h` lists exactly 50, `ITEM_TM51`-`ITEM_TM100` are unused reserved item slots. Per your call, the pool isn't limited to those original 50 moves — each of the 50 TM slots draws a unique move from *every* move in the game (e.g. TM17 could end up teaching Dragon Dance, something that was never TM-teachable before), picked via a seeded unique-draw (bit-vector rejection sampling) in `BuildRandomizedTmMoves`/`src/randomizer.c`. The one carve-out: HM moves (`IsHMMove`, generated from the same `FOREACH_HM` list) are excluded from the TM pool entirely, so HMs stay exclusively HM-taught rather than becoming reachable through two different items with no single correct reverse-lookup answer. `MOVE_STRUGGLE` is also excluded (forced/no-PP special move, was never meant to be teachable). Wired into `GetItemTMHMMoveId`/`GetTMHMItemIdFromMoveId` in `include/item.h` — both were `static inline` compile-time switch statements, so they're now thin wrappers around the original (renamed `_Raw`) switch plus the randomizer, meaning every caller (teaching a TM, describing what a TM does, and `CanLearnTeachableMove`'s "is this move mapped to some TM/HM slot" check from earlier) sees the shuffled mapping with no other changes needed. The reverse lookup (move -> TM item) checks the randomized pool first regardless of whether that move was *originally* a TM, which matters now that non-TM moves can land in the pool)
    - [x] Marts do not have the items they can sell randomized (true by default — this pass only touches overworld item-ball/hidden-item pickups, mart shop inventories are untouched)
    - [x] Items an NPC hands over directly (the `giveitem` script command / `Std_ObtainItem`) are randomized too, same exclusions and combined whitelist+TM pool as field items (key items and HMs untouched). New `RandomizeNpcGiftItem` in `src/randomizer.c` shares its pool logic with `RandomizeFieldItem` via the same `RandomizeItemFromSeed` helper, seeded off the map + original item rather than an object event's local id, since a script-driven gift doesn't have one. Hooked in via a new `RandomizeGivenItem` native call (`src/item_ball.c`) inserted at the top of `Std_ObtainItem` in `data/scripts/obtain_item.inc`, before `additem` runs, so the item name shown in the "obtained the item" message also reflects the substitute. Confirmed `Std_FindItem` (item balls/hidden items, the `finditem` command) is a separate script that doesn't route through `Std_ObtainItem`, so this doesn't double-randomize field items that already went through `RandomizeFieldItem` upstream)
- [x] Pokemon are level capped
    - They cannot level pas the following caps:
        - 0 badges earned: 15
        - 1 badges earned: 19
        - 2 badges earned: 24
        - 3 badges earned: 29
        - 4 badges earned: 31
        - 5 badges earned: 33
        - 6 badges earned: 42
        - 7 badges earned: 50
        - 8 badges earned: 63
    - [ ] Trainers are adjusted for the new level caps (not yet done — trainer parties still use vanilla levels)
    - Tested via debug menu (badge-flag toggling across all 9 tiers)
- [x] Players are given key items for QOL
    - [x] When leaving Littleroot for the first time when players would normally receive sneakers, they instead receive the following key items (all 4 are new `POCKET_KEY_ITEMS` entries, `ITEM_INFINITE_REPEL`/`ITEM_PORTA_HEAL`/`ITEM_ENDLESS_RARE_CANDY`/`ITEM_CAP_CANDY` in `include/constants/items.h`/`src/data/items.h`, `price = 0`/`importance = 1` so they can't be sold or tossed; `LittlerootTown_EventScript_GiveRunningShoes` in `data/maps/LittlerootTown/scripts.inc` now `giveitem`s all 4 instead of switching the player's shoes, with new dialogue explaining the kit):
        - [x] An Infinite Repel (`ITEM_INFINITE_REPEL`, `ItemUseOutOfBattle_InfiniteRepel`/`src/item_use.c` — a reusable bag-menu toggle for `FLAG_INFINITE_REPEL_ACTIVE`, mirroring the existing `ItemUseOutOfBattle_ExpShare` toggle pattern. Deliberately doesn't touch `VAR_REPEL_STEP_COUNT` at all — instead `IsWildLevelAllowedByRepel`/`src/wild_encounter.c` now also passes when this flag is set, so it behaves exactly like a permanently-active Repel (still only suppresses encounters below the lead party member's level) while a real Repel/Lure item can still be tracked and expire normally underneath it. Known gap: two peripheral `REPEL_STEP_COUNT` checks in `src/wild_encounter_ow.c` (overworld-encounter spawn-collision avoidance/timing, not the actual "does an encounter happen" gate) weren't touched, since the real suppression already goes through `IsWildLevelAllowedByRepel`)
        - [x] A porta heal that heals all of their Pokemon to full HP, PP and status (`ITEM_PORTA_HEAL`, `ItemUseOutOfBattle_PortaHeal`/`src/item_use.c` — just calls the existing `HealPlayerParty()`, the same full-party-heal function a Pokémon Center counter uses, so it's usable anywhere, anytime, with no new heal logic needed)
        - [x] An endless Rare Candy (`ITEM_ENDLESS_RARE_CANDY` — reuses Rare Candy's own `fieldUseFunc`/`ItemUseOutOfBattle_RareCandy` and `ExecuteTableBasedItemEffect` path unchanged; `ItemUseCB_RareCandy`/`src/party_menu.c` just skips both `RemoveBagItem` calls for it, so it behaves identically to a real Rare Candy — move-learn prompts, evolution, the level cap gate — except it's never consumed)
        - [x] A cap candy that instantly brings a Pokemon to the level cap (`ITEM_CAP_CANDY`, also routed through `ItemUseOutOfBattle_RareCandy`/`ItemUseCB_RareCandy`. Sets EXP directly to `GetCurrentLevelCap()`'s threshold via `gExperienceTables` instead of the usual +1 level, then falls through into the *same* per-level move-learn loop Rare Candy already uses (`Task_TryLearningNextMove`, which walks `MON_DATA_LEVEL` from the starting level to the target one level at a time) — so every intermediate level's moves still get offered even though the EXP jump itself happens in one step)
            - [x] It does not skip learning moves or evolving the Pokemon (move-learning is handled level-by-level as described above. Evolution has one known caveat, inherited from Rare Candy's existing design rather than introduced by Cap Candy: `PartyMenuTryEvolution` only checks/evolves once at the very end of the level jump, so a mon that should evolve *twice* within one big jump — e.g. a low-level Bulbasaur capped straight to Lv50 — only evolves to Ivysaur on the first use, not all the way to Venusaur. Nothing is silently lost, though: since Cap Candy is never consumed, `PartyMenuTryEvolution`'s existing "still have this item, return to the party menu" behavior (`CB2_ReturnToPartyMenuUsingRareCandy`, keyed off the shared `fieldUseFunc`) lets the player immediately use it again on the same mon to finish the chain)
            - [x] **Bug found and fixed (reported after a real playtest — a Mareep capped from Lv10 to Lv15 showed Lv15 and learned a move, but the party menu later showed it one level higher than when caught, no evolution happened, and catching a Rattata afterward hung the battle screen):** the real defect was in `Task_LearnNextMoveOrClosePartyMenu`/`src/party_menu.c`, a function shared by every level-up-move-learn flow (TM teaching, Rare/Exp/Cap Candy). It decided whether to keep walking to higher levels via `gPartyMenu.data1 == 1`, but `data1` at that point actually holds the *move* that was just learned or replaced (set by `DisplayMonLearnedMove`/`DisplayMonNeedsToReplaceMove`) — since a learned move's id is essentially never literally `1`, this check always took the "close the menu" branch, silently abandoning the level-walk (and the evolution check) the instant *any* level in the jump taught a move. Regular single-level Rare Candy could hit this too, but it's far more likely with Cap Candy's much larger jumps. The mon was left with its level frozen at whichever level taught a move, while its EXP was already set for the full target level — that level/EXP mismatch is what made the post-battle EXP gain from catching the Rattata cascade through many silent level-ups at once (`TryIncrementMonLevel`/`src/pokemon.c` looping since the stored level looked far below what the stored EXP actually supported), which reads as a hang against the mid-battle UI it's not built to run under. Fixed by checking the level-walk's own already-correct state (`sFinalLevel != 0 && sInitialLevel <= sFinalLevel`) instead of `data1`. Any Pokémon already caught in the bad state from before this fix should self-correct the next time Cap Candy is used on them (it recomputes EXP fresh and `CalculateMonStats` re-derives the correct level from it) — avoid battling with an affected mon until then
    - [x] Players still start with Sneakers (`FlagSet(FLAG_SYS_B_DASH)` added to `new_game.c`'s new-save setup, alongside the randomizer defaults. The existing debug menu "Toggle Running Shoes" entry, already wired to this same flag, still works to turn it back off)
    - [x] Whiting out removes these key items (`RemoveQolKeyItemsOnWhiteOut`/`src/overworld.c`, called from `Overworld_ResetStateAfterWhiteOut` right next to the existing `FollowerNPC_TryRemoveFollowerOnWhiteOut()` — the same single choke point every whiteout path (battle loss, scripted whiteouts) already funnels through. Also clears `FLAG_INFINITE_REPEL_ACTIVE` so the toggle doesn't stay stuck on with the item gone)
- [x] Wild pokemon and trainers pokemon do not give EVs (`src/pokemon.c` `MonGainEVs` — added an early-return gate: EVs are only awarded when `BATTLE_TYPE_TRAINER` is set, the opponent isn't a special/link trainer (`IsSpecialTrainer`), and `GetTrainerClassFromId` on the opponent returns `TRAINER_CLASS_LEADER`. This is the single choke point both post-battle EV-gain callsites in `battle_script_commands.c` already route through, so gating it here covers wild battles and every non-Gym trainer at once. "Gym statics" in the original bullet turned out to just mean the Gym Leader trainer battle itself — Hoenn's 8 gym leaders (Roxanne through Juan/Wallace, including rematches) are the only trainers using `TRAINER_CLASS_LEADER`, confirmed via `src/data/trainers.h`. Vitamins/feathers are a separate item-effect code path entirely and were untouched by this change)
    - [x] The static encounters of gyms DO give EVs (this refers to the Gym Leader trainer battle itself, not a literal wild "static encounter" — see above)
    - [x] Pokemon can get EV's only through vitamins, feathers and Gym Battles
    - Tested via debug menu (Gym Leader battle vs. regular trainer/wild battle, "Check EVs" before/after)
- [x] Starter Pokemon always start with 5 perfect IV's (`src/battle_setup.c` `CB2_GiveStarter` — builds the starter mon itself instead of delegating to `ScriptGiveMon`, then calls `SetBoxMonPerfectIVs(&mon.box, 5)` before handing it to the player. Deliberately done at the give-site rather than via the species' `.perfectIVCount` field, since that field is shared with wild/gift encounters of the same species and would leak the guarantee outside the starter pick). Tested via a fresh New Game + "Check IVs" on the received starter
- [x] Bag is disabled in trainer battles only (still usable in wild battles)
- [x] Battle Style is fixed to SET (instead of SHIFT/"Switch"), can't be changed - no more automatic prompt to switch when the opponent sends out a new Pokémon. `gSaveBlock2Ptr->optionsBattleStyle` default flipped to `OPTIONS_BATTLE_STYLE_SET` in `src/new_game.c` (was `OPTIONS_BATTLE_STYLE_SHIFT`), and the "BATTLE STYLE" row deleted entirely from the Options menu (`src/option_menu.c` — `MENUITEM_BATTLESTYLE`/`tBattleStyle`/`BattleStyle_ProcessInput`/`BattleStyle_DrawChoices` and both `gText_BattleStyleShift`/`gText_BattleStyleSet` strings removed, options window shortened by one row) so there's no UI path left to change it. Confirmed no other read/write site exists for this save field besides the options menu and `battle_main.c`'s `gBattleScripting.battleStyle = gSaveBlock2Ptr->optionsBattleStyle;` (the actual behavior switch, left untouched)
- [x] When a Pokemon faints, it is marked permanently (cosmetic marker only — a permanently-fainted mon still shows its usual status icon everywhere, e.g. party menu list, summary screen, but can still technically be selected/used if the player chooses to. Implemented by co-opting Pokérus's storage rather than adding a new save field: `P_POKERUS_ENABLED` set `FALSE` in `include/config/pokerus.h` (kills all vanilla infection/spread/EV-double behavior, but leaves the underlying per-mon byte, `MON_DATA_POKERUS`, fully readable/writable — that byte survives evolution/trade/egg-hatch/link-battle-sync for free since that plumbing already existed). Added `IsMonPermanentlyFainted`/`MarkMonPermanentlyFainted` in `src/pokerus.c` reading/writing that byte directly (bypassing the now-dead `CheckMonPokerus`/`CheckMonHasHadPokerus` gated helpers). Hooked the mark into `SetValuesOnFaint` in `src/battle_util.c` (the single choke point both player- and opponent-faint handling already route through) — only the player-side branch marks, via `MarkMonPermanentlyFainted(GetBattlerMon(battler))`, so it fires on every player faint in every battle type (wild included). For the visual marker, reused the existing "FNT" (currently-fainted, 0 HP) status icon instead of drawing new art — `GetMonAilment` in `src/party_menu.c` now returns `AILMENT_FNT` whenever `IsMonPermanentlyFainted` is true, regardless of current HP, so a permanently-fainted mon keeps showing as fainted forever even after being healed at a Pokémon Center. `pokemon_summary_screen.c` picks this up for free since it calls the same `GetMonAilment`. The debug menu's existing "Give/Clear/Set Pokérus" tools still directly touch the same raw byte — left as-is, doubles as a handy manual way to flag/unflag a mon for testing). Tested via debug menu ("Faint Pokemon" then "Heal party", confirmed the FNT icon persists in party list, summary screen, and PC box)
- [x] Regirock, Registeel, and Regice become available in their unlocked caves after Rayquaza sends Kyogre and Groudon away in Sootopolis (right before the 8th gym). `VAR_SOOTOPOLIS_CITY_STATE` reaches 5 exactly at that story beat (set in `data/maps/SkyPillar_Top/scripts.inc` when Rayquaza is summoned and sends Kyogre/Groudon away — confirmed via the full state timeline: 1 at Seafloor Cavern awakening, 2 back in Sootopolis, 3 entering Cave of Origin, 4 at Sky Pillar's base, 5 at the top with Rayquaza, 6 only after beating the Gym). Added a `VAR_SOOTOPOLIS_CITY_STATE < 5` check to the cave-entrance-interaction scripts in `data/maps/DesertRuins`, `AncientTomb`, and `IslandCave` (both the middle and side entrance triggers in each) that blocks the braille puzzle with a new shared message (`gText_RegiEntranceSealed` in `data/event_scripts.s`) until that story point is reached
    - [x] They are still randomized (stale note updated — this was blocked on the species randomizer, which is now built. Regirock/Registeel/Regice were never added to the restricted-legendary ban list in `src/randomizer.c` (only Regigigas is on it), so they're already eligible for species substitution like any other non-restricted legendary; no additional code needed, just confirming the dependency is resolved)
- [x] ~~New item called Trade Cable that evolves all Pokemon that would otherwise need to be traded to evolve~~ Not needed — pokeemerald-expansion already ships `ITEM_LINKING_CORD` for exactly this (Kadabra, Machoke, Graveler/Graveler-Alola, Haunter, Boldore, Gurdurr, Phantump, Pumpkaboo forms already evolve on it). Use Linking Cord instead of building a new item.
- [x] All Pokemon that need to evolve by trading a specific item instead evolve when given that item — already true by default for Onix (Metal Coat) and Feebas/Milotic before this hack's Milotic override; no engine work needed
- [x] Pokemon that would normally not be able to evolve without specific conditions are instead changed to level evolutions or easier evolution methods. Implemented across `src/data/pokemon/species_info/gen_*_families.h`. Random-split evolutions use the codebase's existing PID-modulo-100 mechanism (a fixed, per-individual "random" split, same trick the vanilla data already uses for Maushold/Dudunsparce) — even odds for the splits below unless noted otherwise. These Pokemon are:
    - Leafeon (Leaf Stone) — moved onto Eevee's main item list, dropped the Petalburg Woods map-location alternative
    - Glaceon (Ice Stone) — same, dropped the Shoal Cave map-location alternative
    - Kingambit (Level 63) — dropped the "defeat 3 Leader's Crest Bisharp" requirement; found and fixed a second, duplicate "Kingambit (Level 50)" entry later in this same list (removed per your call, since Level 63 already covers it)
    - Mamoswine (Level 47)
    - Lickilicky (Level 43)
    - Tangrowth (Level 43)
    - Ambipom (Level 43)
    - Yanmega (Level 43)
    - Tsareena (Level 36)
    - Naganadel (Level 50)
    - Grapploct (Level 36)
    - Overqwil (Level 43)
    - Wyrdeer (Level 43)
    - Annihilape (Level 50)
    - Fargiraf (Level 32)
    - Appletun/Flapple/Dipplin (Level 29, random 3-way even split) — dropped Tart/Sweet/Syrupy Apple requirement entirely
    - Hydrapple (Level 43)
    - Dudunsparce (Level 32) — kept the existing 99/1 real-game-rarity PID split for Two-Segment/Three-Segment, just moved it off the Hyper Drill move requirement onto a flat level
    - Sirfetch'd (Level 36)
    - Milotic (Level 36) — dropped the beauty/Prism-Scale-trade requirement
    - Palafin (Level 38) — already matched, no change needed
    - Gholdengo (Level 47) — both Gimmighoul forms (Chest and Roaming) updated, dropped the 999-coin requirement
    - Malamar (Level 30) — already matched, no change needed
    - Escavalier (Linking Cord) — was the one non-default trade evolution (partner-species-gated, not item-gated like Kadabra/Machoke/etc.); now `EVO_ITEM, ITEM_LINKING_CORD`
    - Accelgor (Linking Cord) — same fix
    - Mr. Mime/Galarian Mr. Mime (Level 30) — dropped the "must know Mimic" requirement, kept the region check so the regional-variant split still works correctly
    - Mantine (Level 21) — dropped the "Remoraid in party" requirement
    - Urshifu Single Strike (Level 47 while knowing a Dark move) — `IF_KNOWS_MOVE_TYPE, TYPE_DARK`, replacing the Scroll of Darkness item
    - Urshifu Rapid Style Strike (Level 47 while knowing a Water move) — `IF_KNOWS_MOVE_TYPE, TYPE_WATER`, replacing the Scroll of Waters item
    - Pikachu (Level 15) — both regular and Alolan Raichu branches updated, region check preserved
    - Togepi (Level 15)
    - Clefairy (Level 15) — dropped Moon Stone requirement
    - Marill (Level 15)
    - Roselia (Level 15)
    - Chingling (Level 15)
    - Sudowoodo (Level 21)
    - Chansey (Level 15) — dropped the Oval-Stone/daytime requirement
    - Snorlax (Level 24)
    - Jigglypuff (Level 15)
    - Blissey (Level 43)
    - Lucario (Level 29)
    - Crobat (Level 36)
    - Espeon (Moon Stone) — moved onto Eevee's item list, replacing the friendship+daytime condition
    - Umbreon (Sun Stone) — same, replacing friendship+nighttime
    - Sylveon (Shiny Stone) — same, replacing friendship+Fairy-move-known
    - Lopunny (Level 21)
    - Swoobat (Level 22)
    - Leavanny (Level 36)
    - Alolan Persian (Level 28)
    - Silvally (Level 43)
    - Frosmoth (Level 29)
    - Rabsca (Level 29) — dropped the 1000-overworld-steps requirement
    - Goodra (Level 50) — both weather-conditioned branches (rain/fog) collapsed into one, since they both led to the same species anyway
    - Galarian Slowking/Slowking (King's Rock) — both now use King's Rock directly. Also revisited Galarian Slowbro while here: it no longer needs Galarica Cuff either, now a plain Level 37 evolution (same level as regular Slowbro) since Galarian Slowpoke is its own distinct species from regular Slowpoke — Galarica Cuff dropped from the Lilycove shop as fully unused
    - Kleavor/Scizor (Randomly at 43, even split) — dropped the Metal Coat/Black Augurite item requirements
        - [x] Tested va debug and its working
    - Ursaluna/Blood Moon Ursaluna (Random at Level 50, even split) — dropped the Peat Block/Hisui-region/nighttime requirement
    - Armarouge/Ceruledge (Random at Level 36, even split) — dropped the Auspicious/Malicious Armor item requirement
    - Polteageist (Level 36) — both Phony and Antique Sinistea variants keep evolving into their respective Polteageist form, just via level instead of Cracked/Chipped Pot
    - Sinistcha (Level 36) — same treatment for Poltchageist's Counterfeit/Artisan variants and the Unremarkable/Masterpiece Teacup items
    - Archaludon (Level 63) — dropped Metal Alloy requirement
    - Runerigus (Level 34) — dropped the curse-damage-threshold requirement
    - Alcremie (Level 30, random form) — this one needed a scope correction: the codebase actually implements 63 distinct Alcremie species (7 sweets × 9 cream/swirl patterns), not 9. Per your call, narrowed the random pool to the 7 "Vanilla Cream" flavor-base forms (one per sweet), 7-way near-even split (~14-15% each), dropping the hold-item/spin-direction/time-of-day mechanic entirely
    - Basculegion (Level 50) — kept the Male/Female split (that's a real distinct-species mechanic, not a difficulty gate), dropped the recoil-damage requirement
    - Melmetal (Level 63) — Meltan had no evolution data at all by default; added a new `.evolutions` field
    - Hydreigon (Level 63) — was already Level 64, adjusted down by 1
    - Solgaleo/Lunala (Randomly at 53, even split) — dropped the day/night requirement
    - Lycanroc forms (Randomly at 25, 3-way even split) — unified onto the regular Rockruff (previously Dusk form required a separate rare "Own Tempo" Rockruff variant, which would have been awkward to guarantee access to under this hack's design); left the Own Tempo variant's dedicated block as a harmless bonus guaranteed-Dusk path
- [x] All pokemon that could evolve into a regional form, the evolution is determined randomly. Found the root issue: this game is Hoenn-only, so the "player's current region" is always Hoenn — any evolution branch gated on `IF_REGION, REGION_ALOLA/GALAR/HISUI` could never trigger, and the paired `IF_NOT_REGION` branch always won by default, making 12 regional forms completely unreachable (Raichu-Alola, Exeggutor-Alola, Marowak-Alola, Weezing-Galar, Mr. Mime-Galar, Typhlosion-Hisui, Samurott-Hisui, Lilligant-Hisui, Braviary-Hisui, Decidueye-Hisui, Sliggoo-Hisui, Avalugg-Hisui). Fixed by swapping the region conditions for the same PID-modulo even-split mechanism used elsewhere in this pass, preserving any other conditions those branches had (Marowak-Alola keeps its nighttime requirement, Decidueye-Hisui keeps its different level-36-vs-34 requirement). Separately audited every regional-form species itself (Vulpix-Alola, Growlithe-Hisui, Sliggoo-Hisui, etc.) to confirm none evolve into a base/non-regional form — all 17 checked out clean, no bugs found there, so "regional forms only evolve into regional forms" was already true
- [x] Shop in Lilycove that sells all the evolution items (new clerk NPC added to the Department Store 5F, `LilycoveCity_DepartmentStore_5F_EventScript_ClerkEvolutionItems`; sells all stones + special evolution items still needed by any species, including Linking Cord; NPC placed at an inferred-clear tile (11,2) — verify in-game and reposition if blocked)
- [x] Side content removed to streamline playthrough
- [x] Set-up moves have their PP reduced (`src/data/moves_info.h` — `.pp` set directly on each move, replacing any vanilla ternary based on `B_UPDATED_MOVE_DATA`)
    - Acupressure 3
    - Agility 2
    - Autotomize 2
    - Bulk Up 2
    - Calm Mind 2
    - Coil 2
    - Curse 3
    - Dragon Dance 2
    - Growth 2
    - Howl 2
    - Hone Claws 2
    - Meditate 2
    - Nasty Plot 2
    - No Retreat 2
    - Quiver Dance 2
    - Rock Polish 2
    - Shell Smash 2
    - Shift Gear 2
    - Sword Dance 2
    - Take Heart 2
    - Tail Glow 2
    - Tidy Up 2
    - Victory Dance 2
- [x] Restricted legendaries are removed from the pool (this list is `sRestrictedLegendaryFamilies`/`sRestrictedLegendarySpecies` in `src/randomizer.c`, matched 1:1 against the list below — enforced via `IsRestrictedLegendary`, called from `IsSpeciesRandomizable` so none of these can appear as a randomizer substitute anywhere: wild encounters, trainers, starters/gifts)
    - Arceus
    - Kyurem
    - Zacian
    - Zamazenta
    - Eternatus
    - Mewtwo
    - Lugia
    - Ho-Oh
    - Rayqauza
    - Dialga
    - Palkia
    - Giratina
    - Reshirom
    - Zekrom
    - Solgaleo
    - Luala
    - Hoopa Unbound
    - Necrozma
    - Calyrex Rider Forms
    - Kyogre
    - Groudon
    - Regigigas
    - Koraidon
    - Miraidon
- [x] Spore as a move is removed from the pool (deleted every `LEVEL_UP_MOVE(x, MOVE_SPORE)` entry across all 9 `src/data/pokemon/level_up_learnsets/gen_*.h` files — this codebase keeps a per-generation snapshot of each species' learnset, so it had to come out of all 9, not just whichever one is active. Confirmed Spore isn't reachable through any other path either: not a TM/HM move, not in any species' tutor (`teachable_learnsets.h`) or egg-move list, and no trainer in `trainers.party` has it hardcoded onto a preset moveset)
- [x] Innards Out reveals itself as the ability when a Pokemon is sent out with it (`src/battle_util.c` `AbilityBattleEffects` — added a new `ABILITY_INNARDS_OUT` case under `ABILITYEFFECT_ON_SWITCHIN` that just calls `BattleScriptCall(BattleScript_AbilityPopUp)`, following the same `gEffectBattler = gBattlerAbility = battler;` pop-up idiom used by every other switch-in ability reveal in that switch. Previously it only revealed itself when its damage-back effect actually triggered on faint, under `ABILITYEFFECT_MOVE_END`, which is untouched and still fires normally)
- [x] Types are shown next to the Pokemon's name when Battle is selected during a fight. (`include/config/battle.h` — `B_SHOW_TYPES` flipped from `SHOW_TYPES_NEVER` to `SHOW_TYPES_ALWAYS`)

## Implementation Audit

### Already built-in (just flip a flag / one-line edit)

| Feature | Where | Status |
|---|---|---|
| Bag disabled in trainer battles | `include/config/battle.h` — `B_VAR_NO_BAG_USE` now points at `VAR_NO_BAG_USE` (renamed from `VAR_UNUSED_0x404E`); set to `NO_BAG_AGAINST_TRAINER` (wild battles keep the bag) in `src/new_game.c` on new game and re-asserted in `src/overworld.c` `Overworld_ResetStateAfterWhiteOut` after whiteout | Done |
| Level caps | `include/config/caps.h` — `B_LEVEL_CAP_TYPE = LEVEL_CAP_FLAG_LIST`, `B_EXP_CAP_TYPE = EXP_CAP_HARD`, `B_RARE_CANDY_CAP = TRUE`; `sLevelCapFlagMap` in `src/caps.c` updated to the 8 badge thresholds (14/21/24/29/36/43/47/50) with a 63 cap through the Champion | Done |
| Starting money | Not a config flag, but hardcoded to one call: `src/new_game.c` `SetMoney(&gSaveBlock1Ptr->money, 750000);` (lowered from 999,999 for money-cap headroom) | Done |
| Lilycove evolution-item shop | New clerk NPC + `pokemart` item list added to `data/maps/LilycoveCity_DepartmentStore_5F/` (map.json object event + scripts.inc), rather than a one-line edit — no pre-existing accessible mart slot for it. List settled at 24 items (10 stones, King's Rock, Metal Coat, Dragon Scale, Upgrade, Protector, Electirizer, Magmarizer, Dubious Disc, Reaper Cloth, Whipped Dream, Sachet, Deep Sea Tooth, Deep Sea Scale, Linking Cord) — every other special evolution item (Leader's Crest, all 7 Sweets, Black Augurite, Prism Scale, Oval Stone, Cracked/Chipped Pot, Galarica Cuff/Wreath, Tart/Sweet/Syrupy Apple, both Teacups, Metal Alloy, both Scrolls, both Armors, Peat Block) is fully unused now that every consuming species is on a level evolution. Galarica Cuff briefly went back and forth (removed, re-added for Galarian Slowbro, then removed again once Galarian Slowbro also moved to a plain level evolution) before landing here | Done |

### Data edits (existing systems, no new engine, but real work)

| Feature | Notes |
|---|---|
| ~60 species evolution-method overrides | Done. All entries across `src/data/pokemon/species_info/gen_*_families.h` — see the checked-off list above for per-species details. Random-split evolutions use the codebase's existing PID-modulo-100 mechanism (same trick vanilla Maushold/Dudunsparce already use), even splits per your call. Alcremie's scope was corrected mid-implementation (63 actual species, not 9 as originally assumed) and narrowed to a 7-way split per your call. Melmetal needed a net-new `.evolutions` field since Meltan had none. Also fixed a duplicate Kingambit entry and a mislabeled Escavalier/Accelgor "Trade Cable" reference found along the way. |
| Regi caves unlock timing | Done. Gated on `VAR_SOOTOPOLIS_CITY_STATE >= 5` in `data/maps/{DesertRuins,AncientTomb,IslandCave}/scripts.inc` |
| Every Pokémon learns every TM and every HM | Done. Not a bitfield in this codebase — TM/HM/tutor compatibility is an explicit per-species move list (`teachableLearnset`). Fixed in the check function (`CanLearnTeachableMove`) instead of editing every species' list, so it applies uniformly and stays correct regardless of future TM/move changes |

### New systems (no existing hook — real feature work)

| Feature | Why it's new |
|---|---|
| Full species randomizer (wild/starters/trainers, ±10% stat variance, ability whitelist, gym/E4/champion exclusion) | Done. `include/randomizer.h`, `src/randomizer.c`, hooked into `src/wild_encounter.c`, `src/battle_main.c`, `src/starter_choose.c`, `src/item_ball.c`/`src/field_control_avatar.c` (items), and `src/pokemon.c` `GetSpeciesAbility` (abilities). Shedinja/Ditto ability locks done; their type-randomization/stat-spread-randomization halves are separate, not-yet-started features — see checklist above |
| Moveset randomizer (21 moves, STAB/non-STAB/status split, power curve to Lv63) | Done. `RandomizeLevelUpLearnset` hooked into `GetSpeciesLevelUpLearnset` in `src/pokemon.c`; original per-species arrays in `src/data/pokemon/level_up_learnsets/gen_*.h` are untouched and still used as the fallback when this feature is off |
| Starter 5-perfect-IV guarantee | Done. `SetBoxMonPerfectIVs` (the same helper `.perfectIVCount`/`P_LEGENDARY_PERFECT_IVS` uses under the hood) called directly in `CB2_GiveStarter` rather than through the species field, so it's scoped to the starter pick only |
| QOL key-item kit replacing/augmenting the Running Shoes pickup, stripped on whiteout | New items + edit to that event script + a whiteout hook |
| Permanent-faint marking (nuzlocke tracker) | New system — worth checking published decomp nuzlocke patches before writing from scratch |
